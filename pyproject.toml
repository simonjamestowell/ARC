[project]
name = "yaka-public"
version = "0.1.0"
description = "Public proof/materials repo for the YAKA ARC-AGI-3 submission."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
